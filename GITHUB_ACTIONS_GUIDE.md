# 🤖 GitHub Actions - دليل البناء التلقائي

هذا الدليل يشرح كيفية استخدام GitHub Actions لبناء APK تلقائياً من GitHub.

---

## 📋 ما هي GitHub Actions؟

GitHub Actions هي أداة تلقائية تقوم بـ:
- بناء التطبيق تلقائياً عند كل push
- اختبار الكود
- إنشاء APK تلقائياً
- نشر الإصدارات

---

## 🚀 كيفية العمل

### **Workflow 1: بناء تلقائي عند كل push**

**الملف:** `.github/workflows/build_apk.yml`

**ماذا يفعل:**
- يبني APK عند كل push إلى `master` أو `main`
- يحفظ APK كـ artifact يمكن تحميله

**الخطوات:**
1. اذهب إلى GitHub
2. اختر مشروعك `greek_diwan_app`
3. انقر على **"Actions"**
4. ستجد **"Build APK"** workflow
5. كل push سيبني APK تلقائياً

---

### **Workflow 2: بناء وإصدار عند إنشاء tag**

**الملف:** `.github/workflows/release.yml`

**ماذا يفعل:**
- يبني APK و AAB عند إنشاء tag جديد
- ينشئ Release على GitHub مع الملفات

**كيفية الاستخدام:**

#### الطريقة 1: من سطر الأوامر
```bash
cd /home/ubuntu/greek_diwan_app

# إنشاء tag جديد
git tag -a v1.0.0 -m "Release version 1.0.0"

# رفع الـ tag
git push origin v1.0.0
```

#### الطريقة 2: من GitHub
1. اذهب إلى **"Releases"**
2. انقر على **"Create a new release"**
3. أدخل tag name: `v1.0.0`
4. أضف وصف
5. انقر على **"Publish release"**

---

## 📱 تحميل APK

### **من Build Artifacts:**
1. اذهب إلى **"Actions"**
2. اختر آخر build
3. اختر **"app-release"** artifact
4. حمّل الملف

### **من Release:**
1. اذهب إلى **"Releases"**
2. اختر الإصدار
3. حمّل **app-release.apk**

---

## 🔧 الإعدادات المتقدمة

### **تغيير إصدار Flutter:**
في ملف `.github/workflows/build_apk.yml`، غيّر:
```yaml
flutter-version: '3.24.0'  # غيّر الإصدار هنا
```

### **تغيير فروع البناء:**
```yaml
on:
  push:
    branches:
      - master
      - main
      - develop  # أضف فروع إضافية
```

### **إضافة خطوات إضافية:**
يمكنك إضافة خطوات مثل:
- اختبار الكود
- فحص الجودة
- نشر على Google Play

---

## 📊 مراقبة البناء

### **في GitHub:**
1. اذهب إلى **"Actions"**
2. شاهد حالة البناء
3. اقرأ السجلات (logs)

### **الحالات الممكنة:**
- ✅ **Success** - البناء نجح
- ❌ **Failed** - البناء فشل
- ⏳ **In Progress** - جاري البناء

---

## 🐛 استكشاف الأخطاء

### **المشكلة: البناء فشل**
**الحل:**
1. اذهب إلى **"Actions"**
2. اختر البناء الفاشل
3. اقرأ السجلات (logs)
4. ابحث عن الخطأ
5. أصلح الكود
6. اعمل push جديد

### **المشكلة: APK لم يتم حفظه**
**الحل:**
- تأكد من أن البناء نجح
- تحقق من artifact section
- جرّب بناء يدوي محلي

### **المشكلة: خطأ في Flutter**
**الحل:**
```bash
# نظّف المشروع محلياً
flutter clean
flutter pub get

# جرّب البناء محلياً
flutter build apk --release
```

---

## 📝 أمثلة الاستخدام

### **مثال 1: بناء تلقائي**
```bash
# اعمل تعديل على الكود
# ثم اعمل push
git add .
git commit -m "Update home screen"
git push origin master

# GitHub Actions سيبني APK تلقائياً
```

### **مثال 2: إنشاء إصدار**
```bash
# إنشاء tag
git tag -a v1.0.0 -m "First release"

# رفع الـ tag
git push origin v1.0.0

# GitHub Actions سينشئ Release مع APK و AAB
```

---

## 🎯 الخطوات التالية

1. **اختبر البناء:**
   - اعمل push صغير
   - شاهد البناء في Actions

2. **أنشئ إصدار:**
   - أنشئ tag جديد
   - شاهد Release

3. **حمّل APK:**
   - من Artifacts أو Release
   - ثبّت على هاتفك

---

## 📚 موارد إضافية

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Flutter GitHub Actions](https://github.com/subosito/flutter-action)
- [Upload Artifact](https://github.com/actions/upload-artifact)
- [Create Release](https://github.com/softprops/action-gh-release)

---

## ✨ ملاحظات مهمة

✅ GitHub Actions **مجاني** للمستودعات العامة
✅ البناء يستغرق **10-15 دقيقة**
✅ يمكن بناء **APK و AAB** معاً
✅ يمكن نشر على **Google Play** تلقائياً

---

## 🎉 تم الإعداد!

GitHub Actions الآن **جاهز تماماً**!

### الخطوة التالية:
1. اعمل push للكود
2. اذهب إلى **"Actions"**
3. شاهد البناء
4. حمّل APK

---

*تم إعداده بـ ❤️ لتطبيق الديوان اليوناني*
